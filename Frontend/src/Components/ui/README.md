# UI Components

This folder contains reusable UI components for the VARTUL application.

## Components

- Avatar: User profile images with fallback
- Button: Styled buttons with variants
- Badge: Status indicators and labels